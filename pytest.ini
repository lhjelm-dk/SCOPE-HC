[pytest]
testpaths = tests
# Put the project root on sys.path so `import scopehc` works regardless of how
# pytest is invoked (bare `pytest` as well as `python -m pytest`).
pythonpath = .
addopts = -q
